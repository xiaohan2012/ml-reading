\pdfoutput=1
\documentclass{article}


% if you need to pass options to natbib, use, e.g.:
\PassOptionsToPackage{numbers, compress}{natbib}
% before loading neurips_2022


% ready for submission
\usepackage[preprint]{neurips_2022}


% to compile a preprint version, e.g., for submission to arXiv, add add the
% [preprint] option:
%     \usepackage[preprint]{neurips_2022}


% to compile a camera-ready version, add the [final] option, e.g.:
%     \usepackage[final]{neurips_2022}


% to avoid loading the natbib package, add option nonatbib:
%    \usepackage[nonatbib]{neurips_2022}


\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
\usepackage[backref=page]{hyperref}
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{xcolor}         % colors

% Our packages
\usepackage{caption}
\usepackage{subcaption}
\usepackage{multirow} % For hyperparameter table
\usepackage{tikz}
\usepackage{pgfplots}
\DeclareUnicodeCharacter{2212}{−}
\usepgfplotslibrary{groupplots,dateplot}
\usetikzlibrary{patterns,shapes.arrows}
\pgfplotsset{compat=newest}
\usetikzlibrary{shapes,decorations,arrows,calc,arrows.meta,fit,positioning}
\usetikzlibrary{positioning,calc}
\usetikzlibrary{backgrounds,fit}
\usetikzlibrary{arrows.meta}
\usetikzlibrary{arrows}

\pgfplotsset{
    compat=1.15,
    mystylepalette/.style={
        mystyle/.style={mark=*, mark size=2, mark options={solid, draw=white, line width=.5pt}, forget plot},
    label style={font=\footnotesize}, tick label style={font=\footnotesize},
    %y label style={at={(axis.north west)},anchor=south},
    },
}

\usepackage{hyperref}
\usepackage{verbatim}
\usepackage{svg}
\usepackage{listings}
\usepackage{amsmath,amssymb}
\DeclareMathOperator*{\E}{\mathbb{E}}
\usepackage[ruled,vlined]{algorithm2e}


\usepackage{todonotes}
\newcommand{\frank}[1]{\todo[inline,color=yellow]{Frank: #1}}
\newcommand{\sam}[1]{\todo[inline,color=green]{Sam: #1}}
\newcommand{\noah}[1]{\todo[inline,color=orange]{Noah: #1}}
\newcommand{\katha}[1]{\todo[inline,color=white]{Katha: #1}}


%\title{Probabilistic AutoML in a Single Forward Pass}
% Probabilistic
% Causal
% 
\title{Meta-Learning a Real-Time Tabular \\ AutoML Method For Small Data}
%\title{Meta-Learning a Real-Time Tabular AutoML \\ Framework For Small Data}
%  Via A Causal Prior
%\title{Learning Fast Tabular AutoML For Small Data}
% Fast Tabular AutoML For Small Data by Meta-Learning a Simplicity Prior over Structural Causal Models
% \title{Meta-Learned Tabular AutoML for Small Data in a Single Forward-Pass}

%First title: Probabilistic AutoML in a Single Forward Pass
%Learning Probabilistic and Causal AutoML for Tabular Data in a Single Forward Pass
%Single Forward Pass and Probabilistic AutoML for Tabular Data

%Teaching a neural network causality: Learning Probabilistic AutoML on Tabular Data in a Single Forward Pass

%AutoML on Tabular Data in a Single Forward Pass by learning a causal posterior

% Learning a neural network that is a full AutoML Pipeline
% A single neural AutoML in a probabilistic and causal manner

%Learning AutoML for Small Tabular Data with a causal prior

%AutoML framework in a single Neural Network

% Tabular AutoML in a Single Forward Pass
% Tabular AutoML for Small Data in a Single Forward Pass

%\todo[]{Frank: I like this title. Some alternatives, just for the heck of it:\\ ``Transformers Can Do AutoML'' \\ ``Teaching A Network To Do AutoML In A Single Forward Pass''}


% The \author macro works with any number of authors. There are two commands
% used to separate the names and addresses of multiple authors: \And and \AND.
%
% Using \And between authors leaves it to LaTeX to determine where to break the
% lines. Using \AND forces a line break at that point. So, if LaTeX puts 3 of 4
% authors names on the first line, and the last on the second line, try using
% \AND instead of \And before the third author name.

\makeatletter
\newcommand{\printfnsymbol}[1]{%
  \textsuperscript{\@fnsymbol{#1}}%
}
\makeatother

\author{
  Noah Hollmann \thanks{Equal contribution.} \\
  University of Freiburg \&\\
  Berlin Institute of Health \\
  Charit\'e University Medicine Berlin \\
  \texttt{noah.hollmann@charite.de} \\
  \And
  Samuel M\"uller \printfnsymbol{1} \\
  University of Freiburg \\
  \texttt{muellesa@cs.uni-freiburg.de} \\
  \And
  Katharina Eggensperger \\
  University of Freiburg \\
  \texttt{eggenspk@cs.uni-freiburg.de} \\
  \And
  Frank Hutter \\
  University of Freiburg \&\\
  Bosch Center for Artificial Intelligence \\
  \texttt{fh@cs.uni-freiburg.de} \\
}

\begin{document}
%\nolinenumbers
\maketitle


\newcommand{\methodname}[0]{TabPFN}
\newcommand{\priortuning}[0]{differentiable hyperparameter tuning}
\newcommand{\openmlcc}[0]{OpenML-CC18}
\newcommand{\openmlautoml}[0]{OpenML-AutoML}
\begin{abstract}
We present \methodname{}, an AutoML method that is competitive with the state of the art on small tabular datasets while being over 1\,000$\times$ faster. 
Our method is very simple: it is fully entailed in the weights of a single neural network, and a single forward pass directly yields predictions for a new dataset.
Our AutoML method is meta-learned using the Transformer-based Prior-Data Fitted Network (PFN) architecture and approximates Bayesian inference with a prior that is based on assumptions of simplicity and causal structures. The prior contains a large space of structural causal models and Bayesian neural networks with a bias for small architectures and thus low complexity.
Furthermore, we extend the PFN approach to differentiably calibrate the prior's hyperparameters on real data.
By doing so, we separate our abstract prior assumptions from their heuristic calibration on real data.
Afterwards, the calibrated hyperparameters are fixed and \methodname{} can be applied to any new tabular dataset at the push of a button.
Finally, on 30 datasets from the \openmlcc{} suite we show that our method outperforms boosted trees and performs on par with complex state-of-the-art AutoML systems with predictions produced in less than a second.
We provide all our code and our final trained \methodname{} in the supplementary materials.

\end{abstract}

% -------------------------------------
\section{Introduction}
\label{sec:introduction}
% -------------------------------------
%New Intro
%0. Tabular data is important and coming up everywhere in the real-world, especially very fast systems can enable new workflows, NNs so far are not so good on them
%1. AutoML systems (and gradient boosted trees) get more complicated over time, are very slow, don't give balanced predictions / not probabilistically founded
%2. Can we learn the behavior of a full AutoML pipeline with a single model that runs fast on GPU?
%this is how
%a. a probabilistic model with good prior: causal, occams razor
%b. novel search over the prior hyperparameters 
%c. large scale training (Transformers)
% -------------------------------------
Machine learning (ML) algorithms for tabular data are essential for many real-world applications in areas like medicine, finance, research, predictive maintenance or sensor data modelling. %, which is dominated by traditional machine learning methods.
%
While neural networks excel on many ML tasks, Gradient-Boosted Decision Trees (GBDT) still dominate the landscape of supervised ML for tabular data~\citep{GBDT,nnsfortabular2}, largely due to their short training time and robustness.
%This is mainly due to competitive performance, short training times and robustness, however, the user is still left with many design decisions.

%In practical applications, h
Hands-free Automated Machine Learning (AutoML) systems for tabular data have been shown to push the limits of ML even 
%by yielding predictions at the push of a button
further~\citep{thornton-kdd13a,hutter-book19a,erickson-arxiv20a}. %  trading off human time with compute time in a specified time budget.% by automatically taking into account past performance on similar datasets, and by constructing ensembles from the models evaluated during the optimization.
However, to achieve this, AutoML systems automatically evaluate and combine a large set of different ML algorithms, thus requiring substantial resources and becoming increasingly complex. % and take a long time to yield results.
State-of-the-art AutoML tools combine several classifiers, various preprocessing tools, advanced hyperparameter optimization methods~\citep{falkner-icml18a}, meta-learning~\citep{feurer-aaai15a,yang-kdd19a} and ensembling~\citep{escalante-jmlr09a,feurer-nips2015a,olson-gecco16a,feurer-arxiv20v2,erickson-arxiv20a}.
These stacked complexities can lead to system failures on the user side~\citep{erickson-arxiv20a} that can be hard to understand.
%Thus, newer systems add even more complexity to catch these failures~\citep{feurer-arxiv20v2}.

%This complicated setup is run on the machine of the user.
AutoML systems are usually benchmarked for several hours, and in our experiments, an AutoML training took at least minutes. This makes applications such as instant classification in a spreadsheet infeasible.
%
We propose a radical change to this pipeline that slashes the required time to \textit{a single second} and simplifies the application to a neural network (NN) forward-pass, a stable and highly portable standard procedure that could even be run on handheld devices.
%that is highly portable and can be easily parallelized on compute architectures like GPUs.
%We do this by meta-learning a NN to become an AutoML system, which unlike previous work is probabilistically founded.

Our method builds on Prior-Data Fitted Networks (PFNs)~\citep{muller-iclr2022}, which meta-learn to approximate the posterior predictive distribution (PPD) for a prior over supervised learning problems that one can sample data from. 
% The PPD is a prediction distribution for unknown labels in our case, based on Bayesian reasoning.
Approximating the PPD implements prediction under Bayesian principles.
PFNs work for any prior that we are able to sample data from.
By doing so, a prediction algorithm can be meta-learned and complex prior assumptions can be encoded through sampling schemes.
%To construct a PPD approximation with PFNs, one simply has to design a sampling algorithm over the prior distribution.
This fundamentally changes the way learning algorithms are designed.
In the original PFN paper~\citep{muller-iclr2022}, PPD approximation with PFNs was, amongst other experiments, demonstrated on binary classification for very small, balanced tabular datasets with only up to $30$ training examples and very few features.
We build on this work %and combine it with information theoretic ideas
to create a state-of-the-art model for tabular multi-class classification tasks that we evaluate on real-world loads of up to $1\,000$ training examples, $100$ features and $10$ classes.

% To be able to scale to 20$\times$ larger tabular datasets we design a prior to overcome a range of things traditionally problematic for deep neural networks: The correlation among features is unstructured, as opposed to the clear spatial or semantic relationship in images or language data; feature types can be numerical, ordinal, and categorical; Feature values can be missing leading to sparse features; Datasets are typically small.

We use a prior based on Bayesian Neural Networks (BNNs ~\citep{neal-bayes96a,gal-phdthesis16a}) and Structural Causal Models (SCMs~\citep{pearl_2009,causalinferencelearningbook}) to model complex dependencies in tabular data. 
Our prior also takes ideas from Occam\textquotesingle{s} razor: simpler models  with fewer variables have a higher likelihood.
By assuming a causal structure and a notion of simplicity in our prior, the PFN performs Bayesian inference over a space of hypotheses that is modelled through a structural causal framework and weighted by simplicity.

Finally, since there are many decisions to be made when building a prior like this, we broadly hyperparameterize our prior, learn a single NN to support this broad prior and accept hyperparameters of the prior during inference time. We introduce a gradient-based methodology to tune these hyperparameters post hoc on real (meta-validation) datasets. 
% in a fully trained neural network.
%and even allow gradient-based methods to tune these hyperparameters. 
This simplifies the development of PFN priors and reduces the number of models to be trained to one.

In this paper, we make the following contributions:
\begin{itemize}
    \item We present a state-of-the-art AutoML method for small, tabular datasets that yields more than $1000\times$ speedup over previous methods.
    \item We design a Bayesian PFN prior on the space of Structural Causal Models, taking into account millions of models during training.
    %\item We show that mixing different priors is viable for PFNs and improves real-world performance.
    \item We introduce \priortuning{} for PFNs, which allows broad and flexible priors that can be refined on a set of real (meta-validation)
    datasets with gradient descent.
\end{itemize}

% 30s per small dataset is usual in SOTA, we do 0.5s
% half a minute to half a second 

\section{Prior-Data Fitted Networks}
PFNs leverage large-scale ML techniques to enable fast approximation of a large set of PPDs~\citep{muller-iclr2022}.
PFNs are NNs that accept a variable length training set $D_{train} = \{(x_1,y_1), \dots, (x_n,y_n)\}$ of input vectors and output labels and a query vector $x$ as input and return an estimate of the PPD $p(y|x,D_{train})$.
They rely on a Transformer that encodes each data point in $D_{train}$ as a token \citep{vaswani-neurips17}.
There are two distinct phases in which we use PFNs: the \textit{(meta-)training} phase and the \textit{inference} phase.
%During meta-training, the PFN, is trained on a ref the objective of PPD approximation as a supervised classification problem:
%A PFN is meta-trained by repeatedly drawing artificial datasets from a prior $p(D) \sim p(\theta)$.

\paragraph{Meta-Training} During meta-training the PFN $q_{\theta}$
%, with weights $\theta$,
is trained to make probabilistic predictions for a held-out example $(x,y) \in D$ based on the rest of a dataset $D_{train}$, where $D$ is an artificial dataset drawn from a pre-defined prior $p(D)$.
The PFN is trained to minimize the following expectation over sampled datasets:
\begin{equation}
    \mathcal{L}_{\textit{PFN}} = \E_{\{(x,y),D_{train}\} \sim p(D)} [q_{\theta}(y|x,D_{train})]
    \label{eq:lpfn}
\end{equation}
For efficiency reasons, we use more than one hold-out example per dataset $D$ in practice. We detail the full training setup in Algorithm \ref{alg:prior-fitting} in the appendix.
Crucially, this \textit{(meta-)training} phase is performed only once for a given prior $p(D)$, learning to do \textit{inference} on any dataset in a single forward pass.

\paragraph{Inference} During the inference phase PFNs approximate the PPD, to make predictions for an unseen real
%-world 
dataset.
The entire dataset and held-out samples are fed as inputs to the PFN and predictions are made per held-out sample.
Thus, training and prediction is done in one single forward pass (similar to prediction with Gaussian Processes), which is different from the standard pipeline in ML, where we train and then evaluate in two distinct phases.


%------------------------------------------------
\section{A Prior for Tabular Data}\label{sec:tabprior}
%------------------------------------------------
% Talk about needs strong priors ?

The performance of our method crucially depends on the specification of a suitable prior, as the PFN approximates the PPD for this prior.
%The prior describes the hypothesis we have about the structure of the input data and their relationship to the output labels. %Alternatively we can think of the data generating processes behind real world tabular data and a priors\textquotesingle{} plausibility in light of these processes.
In the Bayesian framework for supervised learning, the prior defines a space of hypotheses $\Phi$ on the relationship of the inputs to the output labels.
In this work we consider a hypothesis space over Structural Causal Models (SCMs) and Bayesian Neural Networks (BNNs). \methodname{} learns to approximate integration over the space of hypotheses $\Phi$ (i.e., SCMs and BNNs), where the weight of a hypothesis is determined by its prior probability (which we chose to generally align with simplicity, see Section \ref{sec:modelling simplicity assumptions}) and the likelihood of the observations given this hypothesis.
%\begin{align}
%    p(y|x,D) \propto \int_{\Phi} p(y|x,D,\phi) p(D|\phi) p(\phi) d\phi.
%\end{align}
Training a PFN only requires to specify a prior sampling scheme $p(D)=\E_{\phi \sim p(\phi)}[p(D|\phi)]$ and generate samples with $\phi \sim p(\phi)$ and then $D \sim p(D|\phi)$.

%Ensemble learning, where we take the average over multiple model outputs, is a well-known method for improving performance and a rough posterior approximation commonly used for Bayesian Neural Networks.
%For ensembles of neural networks it was recently shown that ensembles over different architectures and setups are outperforming classical ensembles over different initializations of one architecture \citep{nes,hyperensemble}. 
%For traditional ensembles, expensive methods have been created to find a good set of ensemble members for a particular dataset \citep{nes}.
%Ensembles of members with different architectures and hyperparameter settings tend to improve performance, but optimizing the settings for its members can be expensive \citep{wenzel_neurips20,nes}.
%Our model supports the integration of similar ideas cheaply, since
%by mixing different priors we obtain a posterior that is the ensemble of the posteriors for each prior:
%\begin{align}
%    &\E_{\psi}[p(\theta|D,\psi)] = \sum_{\psi} p(\psi) p(\theta|D,\psi)  &&\\
%    &= \sum_{\psi} p(\psi) \frac{p(D|\theta,\psi)p(\theta|\psi)}{p(D|\psi)} && \\
%    %not ready, got stuck
%\end{align}
%\frank{Is this actually true? Due to the likelihood being different for each prior you'd at least have a data-likelihood weighted mix of posteriors, no?}
%\sam{@frank: A mix of priors is equivalent to a mix of posteriors, do you mean that?
%Frank: but with non-uniform weights, right?}
%\sam{I thought so, too, but just yesterday calculated it (not super sure about the correctness), but I think not. I think it is with uniform weights, or in general the weights chosen in the mix of priors.}
%\sam{I will write down the derivation in a very short form, ok? (this will take some time) we can move it to the appendix later.}
This simple setup enables ensembling different kinds of priors
%(i.e.\ modelling assumptions)
and hyperparameters: all we need to do is to define our prior over the hypotheses $\Phi$ as $p(\phi) = \E_{a \sim p(a)}[p(\phi|a)]$.
Here, $a$ could, for example, be a random variable defining the architecture of a BNN (as done by~\citet{muller-iclr2022}). 
%, or it could also define a choice between different types of completely different models. The probability of each prior $p(a)$ is a hyperparameter in general.
In Section~\ref{sec:modelling simplicity assumptions}, we extend this approach to a mixture of a BNN and a structural causal prior,
%(Section \ref{sec:causal prior}) and a BNN prior (Section \ref{sec:bnn prior}), 
each of which entails a mixture of architectures and hyperparameters by themselves.
%
%Additionally, we train on mixtures of the hyperparameters for each prior.
%After training we use \priortuning{} (Section \ref{sec:differentiable hyperparameter tuning}) to tune the probabilities of all hyperparameters and prior models.

In the remainder of this section, we discuss the components of our prior.
We describe additional refinements to our prior to reflect tabular data better in Appendix \ref{sec:tabular data refinement}.
%
\begin{figure}[t]
     \centering
     \begin{subfigure}[b]{0.3\textwidth}
         \centering
         \input{figures/prior_graphs/bayesgraph}
%         \caption{}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.3\textwidth}
         \centering
         \input{figures/prior_graphs/causalgraph}
%         \caption{}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.3\textwidth}
         \centering
         \input{figures/prior_graphs/meta_causalgraph}
%         \caption{}
     \end{subfigure}
        \caption{Left: A BNN. Center: A SCM. Right: Samples from a prior over SCM architectures; PFNs consider a distribution over different model architectures at once. Center and Right: The inputs $x$ are mapped to the output $y$ (grey nodes) with independent noise variables $\epsilon$ added to all nodes. }
        \label{fig:structural_graphs}
\end{figure}
\subsection{Simplicity and Causal Mechanisms}
\label{sec:simplicity and causal mechanisms}

\paragraph{Simplicity}
\label{sec:modelling simplicity assumptions}
We base our priors on a notion of simplicity, such as stated by Occam's Razor or the Speed Prior \citep{speedprior}. When considering competing hypotheses, the simpler one, e.g. the one requiring less parameters, is to be preferred.
Work in cognitive science has uncovered this preference for simple explanations in human thinking as well~\citep{explanatoryvalues}.
A notion of simplicity, however, depends on choosing a language to define simplicity in.
In the following, we introduce prior models based on SCMs and BNNs, in which simplicity materializes as graphs with few nodes and parameters.
Thus, our model prefers hypotheses based on simple SCMs or BNNs during inference capturing the notion of simple explanations.
% We interpret simplicity in a Bayesian framework, sampling models with a lower number of parameters more frequently than models with a higher number of parameters.
%In the context of structural causal models and BNNs this is done by sampling the number of layers and hidden weights from a log normal distribution, which is inherently skewed towards zero. Additionally in our SCMs we choose the probability of dropping connections to follow a beta distribution which gives a tendency towards sparse graphs.

\paragraph{BNN Prior}
\label{sec:bnn prior}
For the first part of our prior, we use the BNN prior as introduced by \citet{muller-iclr2022}.
%We begin by introducing a prior based on Bayesian Neural Networks (BNNs) from \citep{muller-iclr2022}.
To sample a dataset from this prior, we first sample a 
%(noisy) 
NN architecture and its weights.
Then, for each data point in the to-be-generated dataset, we sample an input $x \sim \mathcal{N}(\mathbf{0},\mathbf{I})$,
%a standard normal, 
feed it through the network and use the output $y$ as a target (see Figure~\ref{fig:structural_graphs})\footnote{We note that, while tabular data is not always normalized, we apply z-normalization to the columns.}.
This is a more general setup than in standard BNNs, as the posterior also considers a distribution over architectures with a bias towards simple architectures, not only the standard distribution over a fixed architecture's weights.
Details of this method can be found in \citet{muller-iclr2022}.
%In this prior uncorrelated features are generated from a Gaussian distribution and a class label is generated by feeding features as inputs to randomly sampled Bayesian neural networks (i.e. including Gaussian noise) with diverse architectures and observing the ouput. This yields a distribution over architectures in the posterior. Details of this method can be found in \citep{muller-iclr2022}.
\paragraph{Structural Causal Prior}
\label{sec:causal prior}
%
%
\begin{figure}[t]
     \centering
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         %\def\svgwidth{0.7\columnwidth}
            \includegraphics[width=0.7\columnwidth]{figures/prior_samples/pc4_correlation_matrix.pdf}
%         \caption{}
     \end{subfigure}
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         %\def\svgwidth{0.7\columnwidth}
            \includegraphics[width=0.7\columnwidth]{figures/prior_samples/prior_samples_correlation_matrix2.pdf}
%         \caption{}
     \end{subfigure}
     \vspace*{-0.2cm}
     \caption{Feature Correlation matrices, brighter colors indicate higher correlation. Left: Real world data from the ``PC4 Software defect prediction'' dataset. Right: Synthetic data generated by a causal prior with blockwise sampling as explained in Appendix \ref{sec:tabular data refinement}.}
     \label{fig:feature correlation example}
\end{figure}
%
It has been demonstrated that causal knowledge can facilitate various ML tasks, including semi-supervised learning and transfer learning \citep{scholkopf2012causal}.
Here, we extend the BNN prior above to represent a subclass of SCMs~\citep{pearl_2009, causalinferencelearningbook} as visualized in Figure \ref{fig:structural_graphs}.
An SCM %$\mathcal{G} := (Z, \epsilon{})$
over a set of random variables $Z = \{z_1,\dots,z_m\}$ is defined as a set of structural assignments (called mechanisms), where each variable $z_i$ is defined by a deterministic function
\begin{align}
    z_i = f_i(Z_{\text{PA}_{\mathcal{G}}(i)}, \epsilon_i),
\end{align}
where $\text{PA}_{\mathcal{G}}(i)$ is the set of parents of $z_i$ (its direct causes) in an underlying DAG $\mathcal{G}$ and $f_i$ is a (potentially non-linear) deterministic function.
Variables in $Z$ can be observed (endogenous), i.e., be part of the set of features or labels, or unobserved (exogenous). We make the common assumption that $\mathcal{G}$ does not contain cycles. Thus, relationships can be represented by a directed acyclic graph (DAG), called the causal graph induced by $\mathcal{G}$, with edges $E$ pointing from causes to effects.

We use a prior for simplicity over SCMs by preferring a low number of connections and nodes in the SCM\textquotesingle{s} DAG. We use a subfamily of SCMs as described in the following sampling scheme. There are no theoretical constraints on the subfamily of SCMs other than that we can sample from it.
We chose this framework since (1)~tabular data sets often exhibit causal relationships between columns, and (2)~causal mechanisms have been shown to be a strong prior in human reasoning \citep{waldmann2013causal, explanatoryvalues}.

%In causal inference one seeks to discover causal relations between the components of a larger systems by the use of interventions and observational data \citep{pearl2010causal} while causal discovery seeks to reveal causal information by analyzing purely observational data \citep{zhang2018learning}.  These causal relationships are commonly modelled through SCMs that can be used to make predictions on new observations \citep{pearl_2009}. A Directed Acyclic Graph (DAG) is used to describe relationships between the observed variables included within an SCM (could explain more about other parts?). 
%The majority of existing work in causal inference and discovery focuses on determining a single directed acyclic graph (DAG). Due to the non-identifiability of the causal models without interventional data \citep{eberhardt2007interventions}, causal inference demands reasoning about its uncertainty when given finite observational data.
While we do not perform causal inference, we use a prior over SCMs, to yield a PFN approximating a PPD that is compatible with causal relationships in the data and exhibits strong real-world performance.
%While Bayesian causal inference allows to do so, the posterior over DAGs becomes intractable even for a small number of variables.
This is not easy to do with traditional Bayesian methods, i.e.\ Variational Inference and Markov Chain Monte Carlo, as the hypothesis space is a distribution over many graphs. Thus, a representation of an infinitely large space of hypotheses and their uncertainty would need to be modelled by these methods. While \citet{annadani2021variational} extends this representation to the space of discrete DAGs it is limited to few subfamilies of SCMs and the computational complexity is high.
%For these methods, one would have to instantiate the graphs themselves at inference time, which is an exponentially growing space in the number of nodes under mild assumptions \cite{annadani2021variational}.
%\frank{What precisely is hard to do with VI and MCMC? Why does it require instantiating graphs at inference time? Why do we have to instantiate all the graphs?}
%\sam{The hypothesis/latent for these SCMs and also for distributions over architectures contain many different architectures. If one wants to instantiate this space, one has to sample from a very large space. Not very sure if that means many samples are needed though, I will make it a little less aggressive.}

%In this work, we seek to approximate the PPD directly given a prior over the space of SCMs and therefore we remove the need for finding explicit causal representations for performing predictions.
% Candidate for shortening
PFNs provably approximate Bayesian inference, effectively reasoning over the full space of hypotheses, i.e.\ graphs of SCMs, and their respective weights in a single forward pass.
%(see Figure \ref{fig:structural_graphs}).
%This yields a distribution over graphs of SCMs in the posterior. The weight of each SCM graph hypothesis is determined by the likelihood of observing data given this hypothesis and the likelihood of this hypothesis in our prior.
% This claim is very specific to the comparison to models that instantiate the posterior, but we don't talk about these methods at all. Would need some intro to them to leave it here, I think.
%They propose a variational family for causal structures on which approximate Bayesian inference can be performed. However the considered particular space of SCMs is limited in order to find a tractable variational family using the ELBO. Furthermore the number of nodes considered in the prior is limited by an exponentially growing runtime.
%The approach also seeks to build a causal representation explicitly that could be used for downstream tasks such as prediction but does not deal with approximating the Posterior Predictive distribution.
%Thus our method leverages a prior over causal structures, to enhance predictive reasoning. However we do not perform interventions or seek to answer interventional queries.

%While previous work has been done on considering a space of SCMs, those are restricted by using additive functions in their modelling \citep{lorch2021dibs,annadani2021variationalcausal}.
\paragraph{The Sampling Algorithm}
We instantiate a subfamily of DAGs that can be efficiently sampled from by starting with an MLP-architecture and dropping weights from it.
That is, to sample a dataset with $k$ features and $n$ samples from our prior we perform the following steps for each dataset: \\
\hphantom{~~~~}(1) We sample the number of MLP layers $l \sim p(l)$ and nodes $h \sim p(h)$ and sample a graph $\mathcal{G}$($Z$, $E$) structured like an $l$-layered MLP with hidden size $h$.\\%, $p(l)$ and $p(h)$ are hyperparameters.\\
\hphantom{~~~~}(2) We sample weights for each Edge $E_{ij}$% between $i$ and $j$
as $W_{i,j} \sim p_w(\cdot)$.\\
\hphantom{~~~~}(3) We drop a random set of edges $e \in E$ to yield a random DAG.\\
\hphantom{~~~~}(4) We sample a set of $k$ feature nodes $N_x$  and a label node $N_y$ from the the nodes $Z$.\\
\hphantom{~~~~}(5) We sample the noise distributions $p(\epsilon) \sim p(p(\epsilon))$ from a meta-distribution.%, $p(p(\epsilon))$ is a hyperparameter.
This yields an SCM, with all $f_i$'s instantiated as random affine mappings followed by an activation. Each $z_i$ corresponds to a sparsely connected neuron in the MLP.

With the above parameters fixed, we perform the following steps for each member of the dataset:\\
\hphantom{~~~~}(1) We sample noise variables $\epsilon_i$ from their specific distributions.\\
\hphantom{~~~~}(2) We compute the value of all $z \in Z$ with $z_i = a((\sum_{j \in \text{PA}_{\mathcal{G}(i)}} E_{ij} z_j) + \epsilon_i)$.\\
\hphantom{~~~~}(3) We read off the values at the feature nodes $N_x$ and the output node $N_y$ and return them.

We sample one activation function $a$ per dataset from $\{ReLU, Tanh, Identity\}$ \citep{nair2010rectified}. The sampling scheme for the number of layers $p(l)$ and nodes $p(h)$ is designed to follow a log-normal distributions, the dropout rate follows a beta distribution and $p(p(\epsilon))$ samples normal distributions with normally distributed mean and standard deviation.
% Thus we propose a prior over a distribution of hyperparameters, entailing the space model architectures, i.e. graph connectivity, embedding size, noise strength, model noise and activation functions.
% that is we pick nodes from the SCM and sample their values given normally i.i.d. causes $C$.
The resulting features and targets are correlated through the generating DAG structure. This leads to conditionally dependent features, dependent through forward and backward causation, i.e.\ targets might be a cause or an effect of features.
In Figure~\ref{fig:feature correlation example} we exemplify the resulting correlations between features and compare them to a real-world dataset and in Figure~\ref{fig:prior sample} in the appendix we showcase scatter plots of the features of this prior for different classes.
%For the evaluation on tabular datasets, we present very general priors over causal and neural networks.
%Knowledge of the structural and causal dependencies that underlie the generation of data is an intuitive way of specifying prior knowledge. Those structural dependencies can be described by probabilistic graphical models (PGM), which express the conditional dependendence of random variables. However, inferring those models is computationally very challenging due to the combinatorially large number of candidate graphs and often only possible with some uncertainty.
%\subsection{Bag of Hyperparameters and Prior Models}
%\frank{At the point of my pass, this seems already integrated. I have not thought about whether it might be helpful to have it separate as here; it would also work well together with the part on differentiable hyperparameter tuning, since the natural question is how you would optimize the ensemble.}
%Ensemble learning, where we take the average over multiple model outputs, is a well-known method for improving performance and a rough posterior approximation commonly used for Bayesian Neural Networks.
%For ensembles of neural networks it was recently shown that ensembles over different architectures and setups are outperforming classical ensembles over different initializations of one architecture \citep{nes,hyperensemble}. 
%For traditional ensembles, expensive methods have been created to find a good set of ensemble members for a particular dataset \citep{nes}.
%We can integrate similar ideas into our model easily by mixing different priors, which yields a posterior that is the ensemble of the posteriors for each prior.
%To ensemble priors all we do is define our prior as $p(\theta) = \E_{a \sim p(a)}[p(\theta|a)]$.
%Here, $a$ could for example be a random variable defining the architecture of a Bayesian Neural Network as done in \cite{muller-iclr2022} for example. The probability of each prior $p(a)$ is a hyperparameter in general.
%We extend the approach from \cite{muller-iclr2022} to a mixture of a causal prior (Section \ref{sec:causal prior}) and a BNN prior (Section \ref{sec:bnn prior}), each of which entails a mixture of architectures in turn again.
%Additionally, we train on mixtures of the hyperparameters for each prior.
%After training we use \priortuning{} (Section \ref{sec:differentiable hyperparameter tuning}) to tune the probabilities of all hyperparameters and prior models.
\subsection{Multi-class Prediction}
\label{sec:multi-class prediction}
So far, the described priors apply only to regression tasks, i.e. return scalar labels. In this work we tackle tabular classification and, thus, we need to transform our regression labels to classification labels. We perform the following steps to map the contious labels $\hat{y}$ to discrete labels:\\
%In order to map the continuous labels $\hat{y}$ generated by the priors described above to imbalanced multi-class labels we perform the following steps:\\
\hphantom{~~~~}(1) We sample the number of classes $N_c \sim p(N_c)$, a hyperparameter.\\
\hphantom{~~~~}(2) We sample the class distribution, by uniformly [0, 1] sampling $N_c-1$ quantile bounds $B_c \sim p(B_c|N_c)$ that define $N_c$ quantile ranges $R$.
For example for $N_c = 2$ the quantile bound $B_c = \{0.5\}$ would define two quantile ranges $\{[0.0, 0.5], [0.5, 1.0]\}$ and thus balanced binary labels.\\
\hphantom{~~~~}(3) We map each scalar label $\hat{y}_i$ to the discrete quantile range label $y_i \gets \sum_{j} B_j < \textit{rank}(\hat{y}_i)/n$, where \textit{rank} maps $\hat{y}_i$ to its rank in $\hat{y}$.\\
\hphantom{~~~~}(4) With probability $p_s$, a hyperparameter, we shuffle the labelling of classes, that is, we remove the ordering of class labels wrt.\ the quantile range.
% For the distribution of the number of classes, $P(N_c)$ is a uniform with an extra $50\%$ probability for the binary case (followed by renormalization). $P(B_c)$ controls the class distribution and is chosen to sample bounds uniformly in the range $[0, 1]$, and $p_s$ is a hyperparameter.

% Candidate for shortening
Defining the prior sampling scheme laid out in the previous sections, a mix of regression priors (Section \ref{sec:simplicity and causal mechanisms}) combined with a way to create classification dataset samples from them (Section \ref{sec:multi-class prediction}), is all that is needed to train a PFN to perform Bayesian predictions on tabular data, i.e. make predictions for held-out examples of unseen tabular datasets.
%We layed out how to define a sampling scheme that samples parameters such as the DAG of an SCM as well as weights and class distributions to define a meta-train dataset.
% Next we sample training data points (X, y) and testing data points ($\hat{X}$, $\hat{y}$) from this meta-dataset and finally fit the PFN over millions of meta-train datasets to reduce the losses on ($\hat{X}$, $\hat{y}$) given (X, y).

\iffalse
%---------------------------------------------
\section{Differentiable Tuning of the Prior\textquotesingle{s} Hyperparameters} %\priortuning{}
\label{sec:differentiable hyperparameter tuning}
%---------------------------------------------
%


A very diverse prior, like the one described above, comes with a lot of hyperparameters for its many sub-components. Specifically, our prior has $35$ hyperparameters (for full details see Appendix \ref{sec:our_setup}).
Tuning this large number of hyperparameters with the standard black-box methods, e.g., random search or Bayesian optimization, would be very expensive, due to the curse of dimensionality \citep{maclaurin-icml15a}.


Instead, we tune the prior's hyperparameters with backpropagation. To be able to do this, when training the PFN, we teach it how to predict with different prior hyperparameters; and then, after training on prior samples, we use a meta-validation set of actual datasets to choose the final hyperparameters.

In Figure \ref{overview_diffable_train_valid} in the appendix, we detail the training. For the generation of each training dataset we first sample $\psi$, the hyperparameters of the prior.
Then, we sample a dataset from the prior given $\psi$ and fit the PFN to each dataset prepending an embedding of $\psi$ as an input token. Thus information about the hyperparameters underlying the prior distribution of each dataset is passed to the network as well.
% The difference compared to standard PFN training is that we feed the hyperparameters $\psi$ to the network, too.
% Thus, the PFN knows what prior distribution to expect each dataset to come from.
The objective becomes minimizing the KL-Divergence to the true posterior for hyperparameters sampled from a fixed distribution $p(\psi)$.

The new objective, thus, is an expectation over cross-entropy terms for many different PPDs:
\begin{align}
	&\E_{\psi \sim p(\psi), (x,D_{train}) \sim p(x,D_{train}|\psi)} \left[\text{CE} \left(p(\cdot|x,D_{train},\psi),q_\theta(\cdot|x,D_{train},\psi)\right)\right]. \label{ppd_across_hypers}
\end{align}
This objective minimizes the KL-Divergence between the true posterior and the approximation.
%\frank{Can we say: ``See Appendix X for details.''? We'd need to add the details there in the next week...}
%\sam{This depends on how much Noah can do after the deadline. I think I won't have the time.}
Therefore, we approximate the true posterior across hyperparameter settings in $p(\psi)$ 
%\frank{Is that so? I guess the reasoning is that, in order for this expected CE to become minimal, it has to become minimal for all $\psi$? But in practice, a large reduction in the CE for one $\psi$ could make up for a small increase in the CE for another $\psi'$. Do we believe that we can show something for every constituent of the expectation? If so, could we also point to the appendix for details and then fill them in?}
%\sam{I agree with your argument. We can't show this for every constituent. But I would argue this is the same situation as that we can't show that we minimize the CE for every dataset for PFNs, but only the mean over them. I formulated it a little less strong.}
%\frank{Great}
and our method can be seen as a ``conditional PFN'' that computes the PPD for a prior with given hyperparameters. We show this with a simple derivation in Appendix \ref{app:diff_proof}, similar to the derivations of \citet{muller-iclr2022}.
%In our case, all of the hyperparameters use the same neural network and most have some overlap in their distributions, thus we make use of the transfer-learning of neural networks to yield good PPD approximations, even when the exact hyperparameters given were not or only rarely seen during training.
%Of course, we cannot hope to see every single combination of our 35 hyperparameters during training; rather, we make use of the generalization properties of neural networks to yield good PPD approximations, even for hyperparameter settings that were not seen during training.
Additionally, we combine this approach with the hyperparameter ensembling approach described in the introduction of Section \ref{sec:tabprior}: Hyperparameters are not fixed values, but are defined as distributions over values in the prior, e.g., categorical hyperparameters are specified as the probability of the categories occurring and are sampled for each dataset.
%limitation: the data might give strong hints as to what prior it is sampled from that we can't erase

%
To allow the PFN to accept $\psi$ as an input, we simply prepend a token, a learned linear projection of $\psi$ to the embedding size of the underlying Transformer.
After training our (conditional) PFN, we have an approximation of the PPD for all $\psi$ in the support of $p(\psi)$.
In our experimental setup, we choose one global hyperparameter setting $\psi^*$ that is then used for all experiments with new (meta-test) datasets.
%the given model.
Since we want to choose $\psi^*$ such that the resulting posterior models tabular datasets well, 
\newcommand{\V}[0]{\mathbb{V}}
we created a set of representative meta-validation datasets $\V$ (detailed in Table \ref{table:reference_datasets_table}). We randomly split $\V$ into a set of 90 datasets $\V_1$ and 60 datasets $\V_2$. Next we optimize $\psi$ using gradient descent on the loss for $\V_1$. That is, we initialize $\psi \sim p(\psi)$ at random and perform gradient descent on $\V_1$ to minimize the cross-entropy on the evaluation examples in all the meta-validation datasets in $\V_1$ given $\psi$.
We visualize this in Figure \ref{overview_diffable_train_valid} in the appendix.
We use $\V_2$ for early stopping in the differentiable hyperparameter tuning procedure. We also use $\V_2$ to choose one final $\psi^*$ out of $N$ independent optimizations from different initializations.
We outline this approach in Algorithm \ref{alg:differentiable_hyperparameter_tuning} in the appendix.

\paragraph{Relation to Prior Work}
Passing hyperparameters during inference is used in the Style Transformer which uses this information to influence the style of the language at inference time~\citep{styletransformer}.
%from which dataset from a set of dataset each sequence comes. 
Our setup, on the other hand, chooses prior hyperparameters and, different from \citet{styletransformer}, we perform a gradient-based optimization of $\psi$ after training the (conditional) PFN in a meta-validation phase, but before seeing any meta-test datasets.
\citet{zhang2022differentiableprompt} optimize some input tokens in a gradient-based fashion to do prompt tuning, these tokens however do not represent hyperparameters in any sense, but instead words.
\citet{mackay2019self} follows a similar approach, but while that method requires intricate adaptations of the network architecture to condition network weights on hyperparameters, our method 
%focuses on optimizing hyperparameters of the prior 
is customized to our specific setup, allowing it to be far simpler and more efficient. 
%It accepts the hyperparameters our method is customized for PFNs and does not require any of the intricate adaptations of networks  of 
\fi

\paragraph{Ablation on Tuning Gaussian Process Hyperparameters}
To demonstrate the efficiency of differentiable hyperparameter tuning, we show that it can successfully recover the right hyperparameters in an example task.
We trained a PFN on a simple Gaussian Process (GP) prior with an RBF kernel, where we sample the output scale and length scale uniformly from $[0,10]$, and the noise from $[0.01,0.5]$.
We group the $y$ values for each meta-train dataset sampled from the prior
%outputs 
%for each dataset 
into the top- and bottom-half and treat this as a binary classification problem.
%We considered datasets with 100 training and 100 evaluation examples. 

After training, we consider a GP prior with fixed hyperparameters as validation distribution (representing both $\V_1$ and $\V_2$ from above) and analyze whether we can recover its hyperparameters with \priortuning{}. We generate samples $S$ from Gaussian Processes using the true hyperparameters $\hat{\psi}$ indicated by the dashed blue line. We report the results in Figure \ref{fig:gp_hp_fitting}.
The cross-entropy of the PFN with different values for each hyperparameter in $\psi$ chosen over its whole range (keeping the others at their optimal value), is given by the solid blue line.
As expected, for all hyperparameters, when the model is provided with the correct value it performs best.
We also performed \priortuning{}, as described above, on all hyperparameters, resulting in the values for each hyperparameter marked by the red lines and recovering the hyperparameter values of our validation distribution almost exactly.

% TODO: limitations of diffable: bad samples lead to worse performance

\iffalse
\begin{align}
	&\int_{\psi} p(\psi) \int_{x,y,D_{train}} p(x,y,D_{train}|\psi)q_\theta(y|x,D_{train},\psi)\\
	&=\int_{\psi} p(\psi) \int_{x,y,D_{train}} p(x,D_{train}|\psi) \int_{y} p(y|x,D_{train},\psi)q_\theta(y|x,D_{train},\psi)
\end{align}
\sam{Should we talk about all the different distributions for $\psi$ here? We could also just leave it out, or to the appendix.}
\begin{lstlisting}
"num_layers": {'distribution': 'meta_trunc_norm_log_scaled', 'max_mean': 6, 'min_mean': 1, 'round': True, 'lower_bound': 2},
\end{lstlisting}
\noah{Learning the prior and pac bayesian boundaries for meta learning, also pac optimal hyper posterior @ Jonas Rothfuss}
\fi

\pgfplotsset{
    mystylepalettehptuning/.style={
        height=4cm,
        width=\textwidth,
    },
}

\begin{figure}[t]
     \centering
     %\vspace{1cm}
     \begin{subfigure}[t]{0.32\textwidth}
     %\hspace*{-1.0cm}
         \centering
         \input{figures/diffable_prior/diff_inferred_params_outputscale}
     \end{subfigure}
     \begin{subfigure}[t]{0.32\textwidth}
     %\hspace*{-1.0cm}
         \centering
         \input{figures/diffable_prior/diff_inferred_params_lengthscale}
     \end{subfigure}
     \begin{subfigure}[t]{0.32\textwidth}
     %\hspace*{-1.0cm}
         \centering
         \input{figures/diffable_prior/diff_inferred_params_noise}
     \end{subfigure}
     \vspace{-.5cm}
     \caption{Estimating the hyperparameters of a Gaussian Process. The solid blue indicates the loss of a PFN given the hyperparameter indicated on the x-axis. The dashed (a) blue and (b) red lines indicate the (a) true and (b) estimated hyperparameters.}
     \label{fig:gp_hp_fitting}
\end{figure}


%------------------------------------------------------
\section{Experiments}\label{sec:experiments}
%------------------------------------------------------

Now, we turn to an empirical analysis of our method for actual classification tasks. We empirically compare our method against commonly used ML and AutoML methods for tabular classification tasks with small data and conduct an ablation analysis to show the importance of our design decisions.

%------------------------------------------------------
\subsection{Evaluation on Tabular AutoML Tasks}\label{ssec:tabresults}
%------------------------------------------------------

First, we empirically analyze the predictive performance of our method
%Katha: Drop since repetitive and to save space
%. For this we use on the \openmlcc{}~\citep{bischl-neuripsdbt21a}, a curated open-source list of medium-sized classification datasets 
and compare against state-of-the-art methods for tabular AutoML. We briefly describe our experimental setup before we turn to discussing the results and provide further details in Appendix~\ref{app:expdetails}.
% I just moved this


\paragraph{Datasets} We use three disjoint sets of datasets in our experiments, all focussing on tabular classification tasks with small data. 
As \textit{meta-test} datasets, we used a set of $30$ datasets from the curated open-source \openmlcc{} benchmark suite~\citep{bischl-neuripsdbt21a}, and as as \textit{meta-validation} datasets $\V_1$ and $\V_2$ we used $150$ datasets from \url{OpenML.org}~\citep{vanschoren-sigkdd14a} that we randomly split into two sets of size 90 ($\V_1$) and 60 ($\V_2$). 
We made sure that none of the datasets in $\V$ are contained in the \textit{meta-test} datasets.
We give a full description of all datasets and how we collected them in Appendix~\ref{app:ssec:datasets}.
Finally, after publishing the weights of the \methodname{}, we evaluated these on a different benchmark, the \openmlautoml{} benchmark suite~\citep{amlb}. We select 6 datasets that fit the limitations of our pretrained model in terms of number of samples, features and classes, and are not a part of our \textit{meta-validation} and \textit{meta-test} sets.
% Katha: The following is mostly in the appendix. I dropped it to save space
%Since we focus on learning from few examples, we limited the \openmlcc{} to $30$ datasets with at most $2\,000$ samples, $30$ features and $10$ classes (see Appendix~\ref{app:ssec:datasets}). For each dataset, we used half of the samples for training and the other half as the unseen test set for evaluation.% and repeated this procedure 5 times.
%filtering out datasets with more than 2,000 samples, 100 features or 10 classes. Of the 72 datasets included in the OpenML CC-18 Benchmark, 30 datasets remain that we use for benchmarking. The list of datasets used can be found in Appendix \ref{}. For each dataset we generate 5 splits of which we use 50\% as training data and 50\% for testing.
%
%To conduct meta-learning and tune the prior hyperparameters, we collected a disjoint set of datasets from the OpenML platform (see Appendix~\ref{app:sec:metadata})
%
\paragraph{Baselines} We compare against five standard ML methods and two state-of-the-art AutoML systems for tabular data. As ML models we considered two simple and fast baselines, \textit{K-nearest-neighbors (KNNs)} and \textit{Logistic Regression (LogReg)}, additionally we considered Gaussian Processes (GPs)~\citep{rasmussen-book06a} and two popular tree-based boosting methods, \textit{XGBoost}~\citep{chen-arxiv20a}, \textit{LightGBM}~\citep{NIPS2017_6449f44a} and \textit{CatBoost}~\citep{prokhorenkova-neurips18a}.
For each ML model, we used $5$-fold cross-validation to evaluate randomly drawn configurations until a given budget was exhausted or $1\,000$ configurations were evaluated (for the search spaces, see Appendix~\ref{app:ssec:spaces}). We then refit the configuration with the lowest ROC AUC OVO error on the whole training set. Where necessary, we imputed missing values with the mean, one-hot encoded categorical inputs and normalized features.
%chose the best performing hyperparameters using random search. We minimize XX using -fold crossvalidation for the given budget and then refit on the whole training set afterwards. 
As more complex but also potentially powerful baselines, we chose two AutoML systems: \textit{AutoGluon}~\citep{erickson-arxiv20a} which combines ML models including neural networks and tree-based models into a stacked ensemble, \textit{Auto-sklearn 2.0}~\citep{feurer-nips2015a,feurer-arxiv20v2} which uses Bayesian Optimization and combines the evaluated models into a weighted ensemble and \textit{LightAutoML}~\citep{DBLP:journals/corr/abs-2109-01528}, which we optimize for fast training by restricting models to LightGBM and linear models.\footnote{Auto-sklearn 2.0 optimizes ROC AUC for binary classification and cross-entropy for multi-class classification (as multi-class ROC AUC is not implemented). AutoGluon optimizes cross-entropy on all datasets since this produced better performance and we thus picked the stronger baseline (and report the other results in Appendix~\ref{app:expdetails}).
%otherwise, AutoGluon failed to completely fit on $N$ splits, diminishing its performance across the entire Benchmark (Results of that run can be found in Appendix \ref{app:addresults}).
}
% TODO: We set both AutoML systems to optimize for ROC AUC.
%  neural networks, LightGBM boosted trees (Keet al., 2017), CatBoost boosted trees (Dorogush et al., 2018), Random Forests, Extremely Randomized Trees, and k-Nearest Neighbors.
% of $[30, 60, 300, 900, 3600]$ seconds
%
\pgfplotsset{
    mystylepalette3/.style={
        height=6cm,
        width=\textwidth,
    },
}
\begin{figure}[t]
     \centering
     \begin{subfigure}[t]{0.32\textwidth}
         \centering
         %\hspace*{-1.9cm}
         \input{figures/tabular_results/roc_over_time}
         %\label{fig:aucovertime}
     \end{subfigure}
     \begin{subfigure}[t]{0.32\textwidth}
         \centering
         %\hspace*{-0.2cm}
         \input{figures/tabular_results/roc_wins_tabular}
         %\label{fig:winsovertime}
     \end{subfigure}
     \begin{subfigure}[t]{0.32\textwidth}
         \centering
         %\hspace*{1.1cm}
         \input{figures/tabular_results/roc_raks_tabular}
         %\label{fig:ranksovertime}
     \end{subfigure}
     \vspace{-.6cm}
     \begin{subfigure}[b]{1.0\textwidth}
         \centering
         \input{figures/tabular_results/legend_tabular}
         %\label{fig:legendovertime}
     \end{subfigure}
     \vspace{.3cm}
     \caption{ROC AUC performance over time. We report mean ROC, mean wins and rank along with the 95\% confidence interval across $5$ repetitions for different time budgets. The blue star indicates performance of \methodname{} with differentiable hyperparameter tuning and the red star indicates performance using random samples of the hyperparameter embedding. Results that take over twice as much time as allotted are not plotted in rank and wins plot.
     %The 95\% confidence intervals over splits are shaded. Our method finishes after 1.0 seconds and does not use any further time budget; KNN and Logistic Regression finish without the full time budget as well, dashed lines indicate no further training. 
     %The taken for \methodname{} is on GPU.
     }
     \label{fig:perfovertime}
\end{figure}
\paragraph{\methodname{}} We follow the architecture used in the original PFN paper~\citep{muller-iclr2022}, which uses a Transformer Encoder \citep{vaswani-neurips17} with a specific attention mask and without positional encodings.
We meta-train the \methodname{} once on the prior described in Section~\ref{sec:tabprior} for 20 hours on 8 GPUs, yielding a meta-learned algorithm that can be used for any new dataset.
We note that while this step is expensive it is done offline, in advance and only once for all, as part of the algorithm development.
We applied the following steps to apply the \methodname{}: 
(1) We train a PFN on the prior as in Section \ref{sec:tabprior}; 
(2) We differentiably tune the prior\textquotesingle{s} hyperparameters as in Section \ref{sec:differentiable hyperparameter tuning};
(3) Similar to modern AutoML systems, we ensemble $10$ instantiations of our \methodname{} with randomly permuted feature dimensions and labels~\footnote{Otherwise, the network has biases we do not want, e.g.\ preferring label $0$ in case of uncertainty; this is resolved by permuting labels. We note that this arises from the prior implementation in this work and could be solved in the prior as well.} and vary preprocessing using a robust or power scaler \citep{yeo2000new}. During inference we also apply z-normalization. Further details and hyperparameters used can be found in Appendix \ref{sec:our_setup}.

\paragraph{Evaluation Protocol} For each dataset and method, we evaluated $5$ repetitions, each with a different random seed and a different train- and test split (all methods used the same split given a seed). To aggregate results across datasets, we report the ROC AUC (ROC AUC one-vs-one (OVO) for multi-class classification) average, ranks and wins including the $95\%$ confidence interval and compare to the performance of the baselines with a budget of $[30, 60, 300, 900, 3600]$ seconds\footnote{When comparing methods to each other for a given time budget in Figure \ref{fig:perfovertime}, we drop methods that take more than 200\% of the time budget requested; some methods also do not use their full budget.}. Our \methodname{} is run $10$ times with the above permutations, taking $1.0$ seconds for the full benchmark on a GPU (or $13.2$ seconds on a single CPU).% and all permutations.


%------------------------------------------------------
\paragraph{Results}
%\label{ssec:expresults}
%------------------------------------------------------
%
We present the achieved AUC ROC as a function of budget in Figure~\ref{fig:perfovertime},
% and the final performance wrt.\ different metrics in Table~\ref{tab:tabular_results_table_small}. 
%
%Considering performance-over-time (Figure~\ref{fig:perfovertime}), we
demonstrating that \methodname{} is dramatically ($1\,000\times$) faster than all the other methods while yielding competitive or better predictions: it makes predictions within a single second (on GPU) that tie with the performance of the best competitors (the AutoML systems) after training one hour, and that dominate the performance achieved by competitors in 5 minutes.
Unsurprisingly, the simple baselines (LogReg, KNN) already yield results with a small budget but perform worst overall for larger budgets.
%(see average rank). 
Boosted tree-based methods (XGBoost, CatBoost) perform better, but are clearly outperformed by the two state-of-the-art AutoML systems. The AutoML systems are the best-performing baselines. Given enough budget ($\ge 15$ minutes) they achieve competitive performance with our \methodname{} (which uses a fixed time budget of 1 second in all comparisons). We find that using differentiable hyperparameter tuning provides small improvements over the tuned hyperparameter embeddings. This shows that while hyperparameter tuning improves performance, our \methodname{} is robust to different hyperparameter choices. We provide further details in \ref{app:ssec:abl_diff}. Also, we evaluate \methodname{} on the disjoint set of small datasets in \openmlautoml{} benchmark, after having released pr-trained models. There we also achieve top performance in terms of mean ROC AUC, cross entropy and accuracy.
%
We provide further detailed results, including results per dataset and other metrics in Appendix~\ref{app:expdetails}.

%\sam{What about moving the result table to the appendix!? is that ridiculous?\\
%Katha: Nope, that's exactly what I would propose to do. Move the table and add a sentence "We provide further details in Appendix~\ref{app:expdetails}}
%Our method strong on ROC, CE is better on Autogluon (true ensemble of different methods), Our method better than all non-ensembling methods.
%Drop because I can't find results on single datasets
%We also find that one some datasets we are much worse, when prior cannot be applied. tic-tac-toe (rank 2), dresses-sales, cylinder-bands



% Resources given: 6GB Memory, 1 CPU on Bosch Cluster - We could still update this to 4 CPUs? Is one realistic?

% \subsubsection{Tree-based Learning (Plagiarized)} DTs are commonly-used for tabular
% data learning. Their prominent strength is efficient picking
% of global features with the most statistical information gain
% (Grabczewski and Jankowski 2005). To improve the performance of standard DTs, one common approach is ensembling
% to reduce variance. Among ensembling methods, random
% forests (Ho 1998) use random subsets of data with randomly
% selected features to grow many trees. XGBoost (Chen and
% Guestrin 2016) and LightGBM (Ke et al. 2017) are the two
% recent ensemble DT approaches that dominate most of the
% recent data science competitions.
% Calibration:
% Using a holdout set we can do post-hoc calibration. Here we work without a holdout set and without post-hoc methods. We could show results after post-hoc.

% \subsubsection{Setup of \methodname{}}
% Resources given: 6GB Memory, 1 CPU on Bosch Cluster and GPU (RTX ??)


%------------------------------------------------------
\subsection{Ablation and Generalization Study}
%------------------------------------------------------
\pgfplotsset{
    mystylepalette2/.style={
        height=.6\textwidth,
        width=.8\textwidth,
    }
}
\begin{figure}[t]
     \centering
     \begin{subfigure}[b]{.49\textwidth}
         \input{figures/diffable_prior/diffable_train_valid.tex}
     \end{subfigure}
     \begin{subfigure}[b]{.49\textwidth}
         \input{figures/model_generalization/model_generalization}
     \end{subfigure}
     \caption{Left: Differentiable Hyperparameter Tuning loss curves of both sets of validation datasets $\V_1$ and $\V_2$: We perform multiple (different colors) gradient-descent optimizations of $\phi$ on $\V_1$ from different random initializations and choose the best performer on $\V_2$. The best configuration is used on test data in our models. Right: Extrapolation performance of our \methodname{} to dataset sizes never seen during training. Maximum number of samples during training was 1024 (dashed red line).}
    \label{fig:diffable_train_valid}
    \label{fig:model generalization}
\end{figure}
\paragraph{Tuning the Hyperparameters of the Tabular Prior}
Figure~\ref{fig:diffable_train_valid} shows the optimization curves during differentiable hyperparameter tuning.
When we compare model results before and after differentiable hyperparameter tuning, we can see clear improvements in cross-entropy. 

\paragraph{Prior Ablations}
We perform ablation experiments for a prior based solely on BNNs, SCMs and a mix of both using a hyperparameter to control the sampling likelihood for either.
The PFNs for each prior are fitted using less compute than in our final experiments, thus their scores generally are slightly worse.
For each, we perform differentiable hyperparameter tuning as described in Section~\ref{sec:differentiable hyperparameter tuning} and report results in Table~\ref{tab:scm bnn ablation}. We can see that the choice of prior is crucial.
The BNN prior, which is similar to the one used in \citet{muller-iclr2022}, provides clearly diminished performance compared to the priors based on causal models.
Additionally, we can see that mixing BNN and SCM prior does not seem to make big difference on our test set compared to a pure SCM prior.

\begin{table}[htbp]
    \centering
    \input{figures/ablations/scm_bnn_ablation}
    \vspace{.3cm}
    \caption{An evaluation of the impact of the prior mixing on the final performance. Our final model was trained in the \textit{SCM + BNN} setting, see Section \ref{sec:tabprior} for details on the priors.}
    \label{tab:scm bnn ablation}
\end{table}

\paragraph{Model Generalization}
The Transformer-based PFN architecture accepts a dataset of any length as input. When training this architecture however, the generated datasets are limited to a maxiumum size of 1024.
% on our prior we limit the sequence length of datasets to 1024 of which a random fraction as used as a training set and the remaining samples as evaluation data.
Here, we create a collection of datasets from the OpenML AutoML benchmark with up to $10\,000$ samples and evaluate on up to $5\,000$ training samples. That is a $5$-fold increase as compared to the dataset sizes seen during meta-training of the PFN.
Surprisingly we find that our models generalize to sizes that were never seen during training, as can be seen in Figure \ref{fig:model generalization}. That is Average ROC AUC significantly improves when more training data is provided.
The list of datasets used and experiment details can be found in Appendix \ref{app:expdetails}.
% TODO?
%Why generalization matters: when training long sequences is costly, also theoretically interesting

%\section{Uncertainty estimation}

\section{Conclusions \& Future Work}
%Categorical features
%Mixture of more models
%Plausible causal structures
%Spped allows for new applications, here we show aleatoric, but also interpretability etc.
%Sequence length, but goes hand in hand with strong priors
%Custom applications on top of causal prior
%Diffable allows to encode more real world knowledge
We have shown how a single Transformer, the \methodname{}, can be trained to do the work of a full AutoML framework for tabular data and can yield predictions in 1 second that are competitive with the performance that the best available AutoML frameworks achieve in 1 hour.
This result is likely to disrupt the field of data science, since it allows for real-time predictions. It also presents a turning point for the computational expense of AutoML, allowing for sustainable, green, AutoML.

\methodname{} still has important limitations: the underlying transformer architecture only scales to small datasets; and our evaluations were done on classification datasets with under 2000 samples, 100 features and 10 classes only (detailed in Appendix \ref{sec:limitations}). This motivates a multitude of exciting follow-ups regarding (1)~regression tasks, (2)~scaling up to large datasets, (3)~inclusion of classical ML model classes in the prior, (4)~ensembling to continue making improvements given more time; (5)~quantification of aleatoric and epistemic uncertainty, (6)~studies of interpretability and robustness; (7)~generalizations to non-tabular data; and (8)~dataset-dependent choices of the prior. The almost instant state-of-the-art predictions of \methodname{} are also likely to give rise to (9)~novel exploratory data analysis methods, (10)~novel feature engineering methods and (11)~novel active learning methods.

\newpage

\begin{ack}
Robert Bosch GmbH is acknowledged for financial support. Also, this research was partially supported by TAILOR, a project funded by EU Horizon 2020 research and innovation programme under GA No 952215.
\end{ack}

%with competitive performance in a fraction of time. 
%Our model, \methodname{}, is likely to enable new applications of AutoML on small tabular datasets, as it yields a $1000\times$ speedup over other comparably performing AutoML methods.
%We have shown how crucial the right choice of prior is to build such a powerful method based on PFNs.
%Future work can of course entail increasing the viable dataset size even further.
%There are multiple viable directions for future work. i) One can try to improve performance with other priors based on the SCMs or e.g.\ based on decision trees. ii) Also, scaling this method up even further, to even larger tables can be an interesting challenge to consider. iii) Lastly, work on regression for tabular data is a viable path for future work.

\bibliographystyle{plainnat}
\bibliography{strings,lib,main,proc}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\iftrue
\section*{Checklist}


%%% BEGIN INSTRUCTIONS %%%
%The checklist follows the references.  Please read the checklist guidelines carefully for information on how to answer these questions.  For each question, change the default \answerTODO{} to \answerYes{}, \answerNo{}, or \answerNA{}.  You are strongly encouraged to include a {\bf justification to your answer}, either by referencing the appropriate section of your paper or providing a brief inline description.  For example:
%\begin{itemize}
%  \item Did you include the license to the code and datasets? \answerYes{See Section~\ref{gen_inst}.}
%  \item Did you include the license to the code and datasets? \answerNo{The code and the data are proprietary.}
%  \item Did you include the license to the code and datasets? \answerNA{}
%\end{itemize}
%Please do not modify the questions and only use the provided macros for your answers.  Note that the Checklist section does not count towards the page limit.  In your paper, please delete this instructions block and only keep the Checklist section heading above along with the questions/answers below.
%%% END INSTRUCTIONS %%%


\begin{enumerate}


\item For all authors...
\begin{enumerate}
  \item Do the main claims made in the abstract and introduction accurately reflect the paper's contributions and scope?
    \answerYes{}
  \item Did you describe the limitations of your work?
    \answerYes{Yes in the introduction (Section \ref{sec:introduction}) and in Appendix \ref{sec:limitations}.}
  \item Did you discuss any potential negative societal impacts of your work?
    \answerYes{See Appendix \ref{sec:impacts}.}
  \item Have you read the ethics review guidelines and ensured that your paper conforms to them?
    \answerYes{}
\end{enumerate}


\item If you are including theoretical results...
\begin{enumerate}
  \item Did you state the full set of assumptions of all theoretical results?
    \answerYes{See Appendix \ref{app:diff_proof}.}
        \item Did you include complete proofs of all theoretical results?
    \answerYes{See Appendix \ref{app:diff_proof}.}
\end{enumerate}


\item If you ran experiments...
\begin{enumerate}
  \item Did you include the code, data, and instructions needed to reproduce the main experimental results (either in the supplemental material or as a URL)?
    \answerYes{Instructions are included in the appropriate experimental sections \ref{sec:experiments} and additional information is provided in Appendix \ref{app:expdetails}. Code will be submitted alongside supplementary materials.}
  \item Did you specify all the training details (e.g., data splits, hyperparameters, how they were chosen)?
    \answerYes{See Section \ref{ssec:tabresults} and Appendix \ref{app:expdetails} for splits and evaluation details, and Appendix \ref{app:ssec:spaces} for setup of baselines and \ref{table:test_datasets_table} for datasets.}
        \item Did you report error bars (e.g., with respect to the random seed after running experiments multiple times)?
    \answerYes{See Section \ref{ssec:tabresults}.}
        \item Did you include the total amount of compute and the type of resources used (e.g., type of GPUs, internal cluster, or cloud provider)?
    \answerYes{See Appendix \ref{app:expdetails}.}
\end{enumerate}


\item If you are using existing assets (e.g., code, data, models) or curating/releasing new assets...
\begin{enumerate}
  \item If your work uses existing assets, did you cite the creators?
    \answerYes{Evaluation Datasets are cited in Appendix \ref{app:expdetails}.}
  \item Did you mention the license of the assets?
    \answerYes{See Appendix \ref{app:ssec:datasets}.}
  \item Did you include any new assets either in the supplemental material or as a URL?
    \answerNA{}
  \item Did you discuss whether and how consent was obtained from people whose data you're using/curating?
    \answerNA{}
  \item Did you discuss whether the data you are using/curating contains personally identifiable information or offensive content?
    \answerNA{}
\end{enumerate}


\item If you used crowdsourcing or conducted research with human subjects...
\begin{enumerate}
  \item Did you include the full text of instructions given to participants and screenshots, if applicable?
    \answerNA{}
  \item Did you describe any potential participant risks, with links to Institutional Review Board (IRB) approvals, if applicable?
    \answerNA{}
  \item Did you include the estimated hourly wage paid to participants and the total amount spent on participant compensation?
    \answerNA{}
\end{enumerate}


\end{enumerate}
\fi


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newpage
\appendix

\section{Limitations}
\label{sec:limitations}
The runtime and memory usage of the Transformer-based PFN architecture used in this work scales quadratically with the number of inputs, i.e. training samples passed. Thus, inference on larger sequences (> $100\,000$) is hard on current consumer GPUs. A growing number of methods seek to tackle this issue and report similar performances while scaling linearly with the number of inputs \citep{zaheer2020big, beltagy2020longformer}. These methods can be integrated in the PFN architecture and thus into \methodname{}.
Furthermore, in our experiments we limit the number of features to 100 and the number of classes to 10 as described in Section \ref{sec:experiments}. While this choice is flexible, our fitted \methodname{} can not work with datasets that go beyond these limits.
% The fitting + predicitng step is fast but when we need to make lots of inference we fit each time

\section{Societal Implications}
\label{sec:impacts}
In terms of broader societal impact of this work, we do not see any foreseeable strongly negative impacts.
However, this paper could positively impact the carbon footprint and accessibility of learning algorithms. The computations required for machine learning research have been doubling every few months, resulting in a large carbon footprint \citep{schwartz2020green}. Moreover, the financial cost of the computations can make it difficult for academics, students, and researchers to apply these methods.
The decreased computational time shown by \methodname{} translates to gains in CO2 emissions and cost, making it available to an audience that does not have access to larger scale computing. 


\section{Details of Prior-Data Fitted Network Algorithm} % TODO: Show how distributions etc can be defined in code
\label{app:pfn}
Algorithm \ref{alg:prior-fitting} describes the training method proposed by \citet{muller-iclr2022} for PFNs.

\begin{algorithm}[H]
\SetAlgoLined
\SetKwInOut{Input}{Input}
\SetKwInOut{Output}{Output}
\Input{A prior distribution over datasets $p(D)$, from which samples can be drawn and the number of samples $K$ to draw}
\Output{A model $q_\theta$ that will approximate the PPD}
 Initialize the neural network $q_\theta$\;
 % $p(x_1,\dots,x_{n+m},y_1,\dots,y_{n+m})$\;
 \For{$j\gets1$ \KwTo $K$}{
  Sample $D \cup \{(x_i,y_i)\}_{i=1}^m\sim p(D)$\; %$x_1,\dots,x_{n+m},y_1,\dots,y_{n+m} \sim p(\dataset{})$\;
  Compute stochastic loss approximation $\bar{\ell}_\theta$ = $\sum_{i=1}^{m}(-\log %q_\theta(y_{n+i}|x_{n+i},x_1,\dots,x_{n},y_1,\dots,y_{n}))$\;
  q_\theta(y_i|x_i, D))$\;
  Update parameters $\theta$ with stochastic gradient descent on $\nabla_\theta \bar{\ell}_\theta$\;
 }
 \caption{Meta-Training of a PFN \citep{muller-iclr2022}}
 \label{alg:prior-fitting}
\end{algorithm}

\section{Details of the \methodname{} Prior}
\label{app:a prior for tabular data}
\begin{figure}[tb]
     \centering
     \begin{subfigure}[b]{0.44\textwidth}
         \centering
         %\def\svgwidth{1.1\columnwidth}
            \includegraphics[width=1.1\columnwidth]{figures/prior_samples/prior_samples_causal_good_1_edit.pdf}
     \end{subfigure}
     \begin{subfigure}[b]{0.44\textwidth}
         \centering
         %\def\svgwidth{1.1\columnwidth}
            \includegraphics[width=1.1\columnwidth]{figures/prior_samples/prior_samples_causal_good_2_edit.pdf}
     \end{subfigure}
     \vspace*{-0.8cm}
     \caption{100 samples generated by our causal tabular prior with 4 features. Each point represents a sample with colors indicating the class label. Each plot off the diagonal indicates the values of two features. The diagonal shows histograms of each class and feature. In a causal prior, features are generated from a common generating graph and so share the same generating processes. This can be seen as features tend to be correlated and seem to be simple transformations of each other.}
     \label{fig:prior sample}
\end{figure}

In this section, we describe additional methods included in our prior. First, we describe mixing in Gaussian Process (GP) priors as another way to diversify the hypotheses approximated by \methodname{}. Next, we describe refinements to our prior for the usage on tabular data. Figure \ref{fig:prior sample} visualizes exemplary samples generated by an SCM prior.

\subsection{GP Prior} % TODO: Move this past the Tabular Data Refinement
\label{sec:pg prior}

Tabular datasets are heterogenous in the kind of data described, i.e.\ the hypotheses space induced by BNN and SCM priors might not be appropriate for all datasets. As described in Section \ref{sec:tabprior}, the Bayesian formulation our \methodname{} also allows for mixing different kinds of priors, generated by a distinct model. In addition to mixing BNN and SCM priors we also mix in a prior based on Gaussian Processes (GPs). As described in the work of \citet{muller-iclr2022} we approximate an RBF Kernel with varying hyperparameters (outputscale, lengthscale and noise) and apply the multiclass labelling described in Section \ref{sec:multi-class prediction}. Our experiments showed no significant improvement with adding a GP prior to the mixture of BNN and SCM priors. However, this approach points out an area of future work in which distinct kinds of prior models such as CNNs could be added to the mix of priors.

\subsection{Tabular Data Refinement}
\label{sec:tabular data refinement}
% How do these priors relate to BIC or other criteria that trade off size and quality?Where is this used later on? I can't find the simplicity in the prior.Why does this not cite a single paper about dataset generation? There is plenty of work on learning datasets distributions and sampling from them and that should be recognized. I'd be surprised if no one has yet done sampling from priors as you do, so this should be discussed.

Tabular datasets comprise a range of peculiarities, e.g.\ feature types can be numerical, ordinal, or
categorical and feature values can be missing leading to sparse features. We seek to reflect these peculiarities in the design of our prior as described in the following sections. 

\subsubsection{Preprocessing} \label{app:preprocessing} During meta-training input data is normalized to zero mean and variance and we apply the same step when evaluating on real data. Since tabular data frequently contains exponentially scaled data, which is not might not be present during meta-training, we apply robust and power scaling during inference \citep{yeo2000new}. Thus, during inference on real tabular datasets the features more closely match those seen during meta-training. This preprocessing time is taken into account when reporting the inference time of our method.
We use only training samples for calculating z-statistics, but use training and evaluation samples to perform mean and std-scaling, thus ensuring that our models receive normalised data. While this setup is frequently encountered in real world applications, we also perform an ablation, where we use only training samples for feature calculating means and standard deviations. These results can be found in Appendix \ref{app:abl_train_scaling}.

\subsubsection{Correlated Features}

Feature correlation in tabular data varies between datasets and ranges from independent to highly correlated. This poses problems to classical deep learning methods \citep{nnsfortabular}.
% In a structural causal model, nodes can be connected to a similar set of input nodes and their values will thus be a linear combination of these features before the application of noise and an activation function.
When considering a large space of structural causal models correlated features of varying degrees naturally arise in our priors.
Furthermore, in real world tabular data the ordering of features is often unstructured, however adjacent features are often more highly correlated than others. We use "Blockwise feature sampling" to also reflect the correlation structure between ordered features. Our generation method of structural causal models naturally provides a way to do this. The first step in generating our SCMs is generating a unidirectional layered network structure in which nodes in one layer can only receive inputs from the preceding layer. Thus, features in one layer tend to be higher correlated. We use this by sampling adjacent nodes in the layered network structure in blocks and using these ordered blocks in our set of features.
% Also inspired by \citep{simon1991architecture} we drop out connectivity in blocks, .. maybe in appendix but this is a detail I believe

\subsubsection{Nan Handling}
In order to model missing values in our prior, we introduce missing values probabilistically during prior training. For each dataset, we sample a binary decision variable $M$, indicating whether it will contain missing features and next sample a fraction $f_m$ of missing values. If $M$ is positive, we drop out a fraction $f_m$ of feature values uniformly at random.
We introduce a binary missing value mask, indicating the positions of missing values, and pass it to our model alongside the feature embedding. We append this mask during meta-training on synthetic data and inference on actual datasets.

\subsubsection{Categorical Features}
Tabular data often includes not only numeric features, but also discrete categorical ones. A categorical feature can be ordered, i.e. the categories represent binned degrees of some underlying variable, or shuffled. We introduce categorical features, by picking a random fraction $p_{cat}$ (a hyperparameter) of categorical features per dataset. Analogous to transforming numeric class labels to discrete multiclass labels, we convert dense features to discrete ones. Also analogous to multiclass labels, we pick a shuffling fraction of categorical features $p_{scat}$ where we reshuffle categories. For details see \ref{sec:multi-class prediction}. Our experiments regarding categorical features however showed no significant improvement. Finding more appropriate ways of handling categorical features could be a line of future research.
% \subsubsection{Fixing prior bias}
% Furthermore the current technical implementation of our sampling setup leads to detrimental biases in the expected class distribution and significance of features based on their position. Biases over the class distribution can be attributed to sampling lower class labels more often than high class labels (e.g. classes one and two will be present in all datasets). Even when a class with a low class label is never observed, observing a higher class value means that this lower class label will be present in the test set due to our algorithm.
% We seek to alleviate these biases by performing class and feature shifts during inference, i.e. we randomly rotate the label and feature ordering. When the same data is predicted across all class and feature rotations these biases even out. This however is costly and so we randomly sample shifts balancing accuracy and speed - the losses in speed and accuracy introduced by this are affecting our results. Fixes to this are possible and will push performance even further.

\section{Details for Differentiable Hyperparameter Tuning}
\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{1.\textwidth}
        \centering
        \input{figures/overview_figures/training}
        %\caption{}
    \end{subfigure}
    \vspace{.3cm}
    \begin{subfigure}[b]{1.\textwidth}
        \centering
        \input{figures/overview_figures/diffhptuning}
        %\caption{}
    \end{subfigure}
    \caption{A visualization of the meta-training in the top and hyperparameter tuning setup in the bottom. We sample hyperparameters (HPs) along with artificial datasets and optimize the HP embedding $\theta$, while we sample real validation datasets during hyperparameter tuning and optimize the style $\psi$.}
    \label{overview_diffable_train_valid}
\end{figure}
\subsection{Derivation of Claims for Differentiable Hyperparameter Tuning}
\label{app:diff_proof}
We can discern three distinct claims in Section \ref{sec:differentiable hyperparameter tuning}.\\
\paragraph{i) Minimizing Equation \ref{ppd_across_hypers} is equivalent to minimizing the KL-Divergence.} This is a simple standard proof regarding the connection between cross-entropy and KL-Divergence. One variant is noted in \citet{muller-iclr2022} in Corollary 1.1.
\paragraph{ii) Our objective is Equation \ref{ppd_across_hypers}.}
In the following we derive that Equation \ref{ppd_across_hypers} actually is the loss we minimize, we broadly follow Insight 1 of \citet{muller-iclr2022}. This derivation starts with $L_{\text{PFN}}$ as defined in Equation \ref{eq:lpfn}, which we extend with hyperparameters as described in Section \ref{sec:differentiable hyperparameter tuning} and derive the loss as described in Equation \ref{ppd_across_hypers}
\begin{align}
	&\E_{\psi \sim p(\psi)}\left[ \E_{(y,x,D_{train}) \sim p(y,x,D_{train}|\psi)}\left[  -\log q_\theta(y|x,D_{train},\psi)\right]\right]\\
	&\E_{\psi \sim p(\psi)}\left[ \E_{(x,D_{train}) \sim p(x,D_{train}|\psi)} \left[ \E_{y \sim p(y|x,D_{train},\psi)}\left[  -\log q_\theta(y|x,D_{train},\psi)\right]\right]\right]\\
	&\E_{\psi \sim p(\psi)}\left[ \E_{(x,D_{train}) \sim p(x,D_{train}|\psi)} \left[ CE(p(y|x,D_{train},\psi),q_\theta(y|x,D_{train},\psi))\right]\right]\\
	&=\E_{\psi \sim p(\psi), (x,D_{train}) \sim p(x,D_{train}|\psi)} \left[\text{CE} \left(p(\cdot|x,D_{train},\psi),q_\theta(\cdot|x,D_{train},\psi)\right)\right].
\end{align}
For most hyperparameters we simply use a uniform distribution as $p(\psi)$, thus we can view this objective as minimizing the average distance for all considered hyperparameters $\psi$.
\paragraph{iii) We approximate the true posterior across hyperparameters.}
This is a trivial insight, given i) and ii).
We know that our loss is equivalent to an expectation over KL-Divergences, up to a constant.
Thus, minimizing our objective will lead to closer solutions in terms of the expectation over KL-Divergences.
Assuming there is a $\theta$ with $p=q_\theta$ we can make this claim tighter.
We know that the KL-Divergence between two distributions $p_1$ and $p_2$ is zero iff $p_1 = p_2$.
Thus, our objective is minimal iff $p=q_\theta$, as shown by \citet{muller-iclr2022} in Corollary 1.2.
This in turn implies equality for all hyperparamters $\psi$.


\def \numdraws {\textit{num\_draws}}
\def \numsteps {\textit{num\_steps}}

\begin{algorithm}[h!]
\SetAlgoLined
\SetKwInOut{Input}{Input}
\SetKwInOut{Output}{Output}
\Input{Two sets of validation datasets $\V_1$ and $\V_2$ and a PFN $q(y|x,D,\psi)$.}
\Output{Final hyperparameters to use the model with $\psi^*$.}
 Initialize the neural network $q_\theta$\;
 % $p(x_1,\dots,x_{n+m},y_1,\dots,y_{n+m})$\;
 \For{$d\gets1$ \KwTo \numdraws}{
    Sample initial $\psi_d^{(0)} \sim p(\psi)$\;
\For{$s\gets1$ \KwTo \numsteps}{
 $g \gets 0$\;
 \ForEach{$D_{train},\{(x^{(1)}_{val}, y^{(1)}_{val}), \dots, (x^{(m)}_{val}, y^{(m)}_{val})\} \in \V_1$}{
  $l \gets \sum_i\text{CE}(q(\cdot|x^{(i)}_{val},D_{train},\psi_d^{(s-1)}),y^{(i)}_{val})$\;
  $g \gets g + \frac{dl}{d\psi_d}$\;
 }
 Take optimizer step with aggregated gradients $\psi_d^{(s)} \gets Adam(\psi_d^{(s-1)}, g)$\;
 }}
 Finally, choose best performer on $\V_2$ from all $\psi_d^{(s)}$ and return it as $\psi^*$.
 \caption{Differentiable Hyperparameter Tuning}
 \label{alg:differentiable_hyperparameter_tuning}
\end{algorithm}

\subsection{Softmax temperature calibration}
In addition to differentiably tuning the prior\textquotesingle{}s hyperparameters, as described in Section \ref{sec:differentiable hyperparameter tuning}, we also introduce a softmax temperature factor, that is differentiably tuned on our meta-train and meta-validation dataset alongside the hyperparameters $\psi$. That is when decoding the logits produced by our \methodname{} to probabilities we perform the following calculation:
%
$$p_{\methodname{}}(logit, t) = \frac{\exp(x_i/t)}{\sum_j \exp(x_j/t)},$$
%
where $logit_i$ is the logit probability for class $i$ and $t$ is the temperature factor. During \priortuning{} we also propagate the cross entropy loss on the meta-train datasets to the factor $t$ and use early stopping on the meta-validation datasets for the best combination of $t$ and prior hyperparameters $\psi$.
\section{Setup of our method}
\label{sec:our_setup}

\subsection{Transformer Hyperparameters} % TODO: @ Sam We might want to make this clearer after review
While, most of our hyperparameters are tuned with differentiable hyperparameter tuning, the hyperparameters of our transformer architecture and training are not part of the prior and can thus not be tuned in the same way.
We considered only PFN Transformers with $12$ layers, embeddings size $512$, hidden size $1024$ in feed-forward layers, and $4$-head attention. For each training we tested a set of 3 learning rates, $\{.001,.0003,.0001\}$, and used the one with the lowest final training loss.

\subsection{\methodname{} Training}
We trained our final model for $18\,000$ steps with a batch size of $512$ datasets. That is our \methodname{} is trained on $9\,216\,000$ synthetically generated datasets. This training takes 20 hours on 8 GPUs (Nvidia RTX 2080 Ti). Each dataset had a fixed size of $1024$ and we split it into training and validation uniformly at random. We generally saw that learning curves tended to flatten after around $10$ million datasets and where generally very noisy. Likely, this is because our prior generates a wide variety of different datasets.

\subsection{Prior Hyperparameters}
The hyperparameters of our prior were tuned using \priortuning{}. For the distributions of the hyperparameters tuned with \priortuning{} see Table \ref{tab:diffable_hps}. Hyperparameters that specify sampling distributions other than uniform are made up of one or more underlying uniform distributions. Choice HPs are made up of one hyperparameter per choice, except for the first choice. Beta distributions are made up of two parameters $\alpha$ and $\beta$. Truncated Normals are made up of a mean and a standard deviation. The $Min$ value is added to the HP to give a lower bound. If Rounding is enabled, Hps will be rounded to the next value after sampling. The model receives the values of each underlying parameter making up these distributions during \priortuning{}.
\begin{table}[]
\small
    \centering
    \caption{Overview of our priors hyperparameters optimized using \priortuning{}.}
    \begin{tabular}{llllll}
    \toprule
    {} & Sampling & Minimum & Maximum & & \\
    {} & distribution $p(\psi)$ &  &  & & \\
    \midrule
    GP sampling likelihood &                         Uniform &     0.5 &     8.0 & & \\
    \toprule
    {} &  & Min $\alpha$ and $\beta$ & Max $\alpha$ and $\beta$ & Scale HP & \\
    \midrule
    MLP weight dropout &                       Beta &                    0.1 &                    5.0 &            0.9 & \\
    \toprule
    {} &  & Choices &  &  &  \\
    \midrule
    Sample SCM &                     Choice &                                      [True, False] & & & \\
    Share Noise mean for nodes &                     Choice &                                      [True, False] & & & \\
    Sample y from last MLP layer         &                     Choice &                                      [True, False] & & &  \\
    MLP Activation Functions                   &                     Choice &  \tiny [Tanh, ReLU, ELU]& & &  \\
    MLP Activation Functions                   &                     Choice &  \tiny , Identity, Threshold] & & &  \\
    Blockwise Dropout                       &                     Choice &                                      [True, False] & & &  \\
    Keep SCM feature order                  &                     Choice &                                      [True, False] & & &  \\
    Sample features blockwise          &                     Choice &                                      [True, False] & & &  \\
    GP noise                                &                     Choice &                              \tiny  [1e-05, 0.0001, 0.01] & & & \\
    \toprule
    {} &  & Max Mean & Min Mean & Round HP & Min \\
    \midrule
    MLP \#layers                 &      Trunc. Normal &        6 &        1 &        True &           2 \\
    MLP \#hidden nodes per layer &      Trunc. Normal &      130 &        5 &        True &           4 \\
    Gaussian Noise Std.          &      Trunc. Normal &      0.3 &   0.0001 &       False &         0.0 \\
    MLP Weights Std.             &      Trunc. Normal &     10.0 &     0.01 &       False &         0.0 \\
    SCM \#nodes at layer 1        &      Trunc. Normal &       12 &        1 &        True &           1 \\
    GP outputscale               &      Trunc. Normal &     10.0 &  0.00001 &       False &           0 \\
    GP lengthscale               &      Trunc. Normal &     10.0 &  0.00001 &       False &           0 \\
    \bottomrule
    \end{tabular}
    \label{tab:diffable_hps}
\end{table}


%\section{Reproducibility}
%Code, ..
% ---------------------------------------------
\section{Details for Tabular Experiments}\label{app:expdetails}
% ---------------------------------------------

Here we provide additional details for the experiments conducted in Section~\ref{sec:experiments} in the main paper. 

% ---------------------------------------------
\subsection{Hardware Setup} \label{app:ssec:hardware}
% ---------------------------------------------
All evaluations, including the baselines, ran on a compute cluster equipped with Intel(R) Xeon(R) Gold 6242 CPU @ 2.80GHz using $1$ CPU with up to $6$GB RAM. For evaluation using our \methodname{}, we additionally use a RTX 2080 Ti.

% ---------------------------------------------
\subsection{Baselines}\label{app:ssec:spaces}
% ---------------------------------------------
We provide the search space used to tune our baselines in Table~\ref{app:tab:spaces}. For \textit{CatBoost} and \textit{XGBBoost}, we used the same ranges as~\citet{nnsfortabular2} with the following exceptions: For \textit{CatBoost} we removed the hyperparameter max\_depth since it is not in documentation, for \textit{CatBoost} and \textit{XGBBoost}, we set the range for n\_estimators to be in $[100, 4000]$. The search spaces for the KNN, GP and Logistic Regression baselines were designed from scratch and we used the respective implementation from \textit{scikit-learn}~\citep{scikit-learn}. For Catboost, AutoSklearn and LightAutoML we pass the position of categorical features to the classifier (AutoGluon automatically detects categorical feature columns). We normalize inputs for Logistic Regression, GP and KNN to the range [0, 1] using MinMax Scaling.
% Hyperparameters taken from https://arxiv.org/pdf/2106.03253.pdf. Catboost max_depth couldn't be found.

\begin{table}[htbp]
\caption{Hyperparameter spaces for baselines. All, except LightGBM, adapted from~\citet{nnsfortabular2}.}
\label{app:tab:spaces}
\centering
\begin{tabular}{lllll}
\toprule
baseline &  name & type & log & range \\
\midrule
\multirow{3}{*}{LogReg} & penalty & cat & (l1, l2, none) & - \\
    & max\_iter & int & [50, 500] & - \\
    & fit\_intercept & cat & (True, False) & - \\
    & C & float & [$e^{-5}$, 5] & - \\
\midrule
\multirow{1}{*}{KNN} & n\_neighbors & int & [1, 16] & - \\
\midrule
\multirow{2}{*}{GP} & params\_y\_scale & float & [0.05, 5.0] & yes \\
    & params\_length\_scale & float & [0.1, 1.0] & yes \\
\midrule
\multirow{6}{*}{CatBoost} & learning\_rate & float & [$e^{-5}$, 1] & yes \\
    & random\_strength & int & [1, 20] & - \\
    & l2\_leaf\_reg & float & [1, 10] & yes \\
    & bagging\_temperature & float & [0, 1.0] & yes \\
    & leaf\_estimation\_iterations & int & [1, 20] & - \\
    & iterations & int & [100, 4000] & - \\
\midrule
\multirow{10}{*}{XGBoost} & learning\_rate & float & [$e^{-7}$, 1] & yes \\
    & max\_depth & int & [1, 10] & - \\
    & subsample & float & [0.2, 1] & - \\
    & colsample\_bytree & float & [0.2, 1] & - \\
    & colsample\_bylevel & float & [0.2, 1] & - \\
    & min\_child\_weight & float & [$e^{-16}$, $e^5$] & yes \\
    & alpha & float & [$e^{-16}$, $e^2$] & yes \\
    & lambda & float & [$e^{-16}$, $e^2$] & yes \\
    & gamma & float & [$e^{-16}$, $e^2$] & yes \\
    & n\_estimators & int & [100, 4000] & - \\
\midrule
\multirow{10}{*}{LightGBM} & num\_leaves & int & [5, 50] & yes \\
    & max\_depth & int & [3, 20] & yes \\
    & learning\_rate & float & [$e^{-3}$, 1] & - \\
    & n\_estimators & int & 50, 2000 & - \\
    & min\_child\_weight & float & [$e^{-5}$, $e^4$] & yes \\
    & reg\_alpha & float & [0, 1e-1, 1, 2, 5, 7, 10, 50, 100] & yes \\
    & reg\_lambda & float & [0, 1e-1, 1, 5, 10, 20, 50, 100] & yes \\
    & subsample & float & [0.2, 0.8] & - \\
\bottomrule
\end{tabular}
\end{table}
% ---------------------------------------------
\subsection{Used Datasets}\label{app:ssec:datasets}
% ---------------------------------------------

To construct and evaluate our method, we use three disjoint sets of datasets. Our test-set (see Table~\ref{table:test_datasets_table}), a subset of the \openmlcc{} benchmark suite~\citep{bischl-neuripsdbt21a} and a meta-set (see Table~\ref{table:reference_datasets_table} and~\ref{table:reference_datasets_table_2}), which we collected from \url{OpenML.org}~\citep{vanschoren-sigkdd14a}. These are licensed under the BSD 3-Clause license.

For the test-set, we considered all datasets in the \openmlcc{} benchmark suite with less than $2\,000$ samples, $100$ features or $10$ classes, which leaves us with $30$ datasets that represent small, tabular datasets.
%
For the meta-set, we considered all datasets on \url{OpenML.org} and applied the following filtering procedure: We dropped all datasets that are in the test-set and all datasets with more than $1\,000$ samples, $100$ features or $10$ classes. We also manually checked for overlaps and removed datasets where the number of features, classes and samples was identical to a dataset in the test-set. Furthermore, we manually dropped FOREX (since it is a time series dataset) and artificially created datasets, such as Univ and Friedman datasets. The remaining meta-set then contains of $150$ datasets.

%filtering out datasets with more than 2,000 samples, 100 features or 10 classes. Of the 72 datasets included in the OpenML CC-18 Benchmark, 30 datasets remain that we use for benchmarking. The list of datasets used can be found in Appendix \ref{}. For each dataset we generate 5 splits of which we use 50\% as training data and 50\% for testing.

% ---------------------------------------------
\subsection{Additional Results}\label{app:ssec:addresults} % TODO: After revision move this to the front
% ---------------------------------------------

In addition to the results in the main paper in Section~\ref{ssec:tabresults}, we report a wide range of performance values in Table \ref{app:table:tabular_results_table_small_1h}. We show quantitative results for comparing all methods when given up to $15min$ time. We see that our model is competitive with strong AutoML baselines while requiring a fraction of that time. Furthermore, we report ROC AUC performance over time, similarly to Figure~\ref{fig:perfovertime}, except that AutoGluon optimizes for ROC AUC. While the mean rank and number of wins remains similar, the average AUC ROC performance decreases in our experiments, due to failure to train Catboost models on $2$ datasets (MiceProtein, mfeat-zernike).
We also report performance on all datasets of the \openmlautoml{} Benchmark that were not contained in either the \openmlcc{} Benchmark or our validation datasets. We add this benchmark posthoc to circumvent overfitting to our meta-test set. Detailed results for this can be found in Table \ref{app:table:tabular_results_table_small_automl}.

% \begin{table}[h]
%     \caption{Average performance on the test-datasets for $15$ minutes ($900$ s) requested time. We report overall \textit{Wins}, \textit{Wins/Ties/Losses (W/T/L)} against \methodname{}, \textit{mean performance (Mean)} and \textit{mean rank (Mean rank)} wrt. ROC AUC, crossentropy loss (CE), and accuracy (Acc). Additionally, we also report the mean time used to fit a single model and obtain predictions.
%     %If available, all Baselines are given ROC AUC optimization as an objective, others optimize CE. Overall each method got a time budget of 37.5 hours, that \methodname{}, KNN, GP and LogReg did not use fully.
%     }
%     \label{tab:tabular_results_table_small}
%     \centering
%     \tiny
%     \input{figures/tabular_results/tabular_results_table_small}
% \end{table}

\begin{table}[h]
    \caption{ROC AUC OVO results on the small \openmlcc{} for 15 minutes requested time per dataset and per split. If available, all baselines are given ROC AUC optimization as an objective, others optimize CE. Overall each method got a time budget of 37.5 hours, but not all methods used the full budget.}
    \label{app:table:tabular_results_table_small_1h}
    \centering
    \tiny
    \input{figures/tabular_results/tabular_results_table}
\end{table}

\pgfplotsset{
    mystylepalette3/.style={
        height=6cm,
        width=\textwidth,
    },
}
\begin{figure}[t]
     \centering
     \begin{subfigure}[t]{0.32\textwidth}
         \centering
         %\hspace*{-1.9cm}
         \input{figures/tabular_results/roc_over_time_autogluon_roc}
         \label{fig:aucovertime_autogluon}
     \end{subfigure}
     \begin{subfigure}[t]{0.32\textwidth}
         \centering
         %\hspace*{-0.2cm}
         \input{figures/tabular_results/roc_wins_tabular_autogluon_roc}
         \label{fig:winsovertime_autogluon}
     \end{subfigure}
     \begin{subfigure}[t]{0.32\textwidth}
         \centering
         %\hspace*{1.1cm}
         \input{figures/tabular_results/roc_raks_tabular_autogluon_roc}
         \label{fig:ranksovertime_autogluon}
     \end{subfigure}
     \vspace{-.6cm}
     \begin{subfigure}[b]{1.0\textwidth}
         \centering
         \input{figures/tabular_results/legend_tabular}
         \label{fig:legendovertime_autogluon}
     \end{subfigure}
     \vspace{.3cm}
     \caption{ROC AUC performance over time, as reported in Figure \ref{fig:perfovertime}. But with Autogluon fails.
     }
     \label{fig:perfovertime_autogluon}
\end{figure}
\begin{table}[h]
    \caption{ROC AUC OVO results on the 6 small datasets (less than 2000 examples, 100 features and 10 classes) of \openmlautoml{}, that are not contained in $V_1$, $V_2$ or the \openmlcc{} benchmark. All methods were given a budget of 15 minutes per dataset and per split. If possible, all baselines are optimized for ROC AUC, otherwise we optimize CE. Evaluation on this benchmark was performed with previously released pretrained models in order to mitigate test-set overfitting.}
    \label{app:table:tabular_results_table_small_automl}
    \centering
    \tiny
    \input{figures/tabular_results/tabular_results_table_automl}
\end{table}
\subsection{Model Generalization}\label{app:ssec:abl_generalization}
For testing the \methodname{} performance on longer sequences, as described in Section \ref{fig:model generalization}, we used a set of datasets from the OpenML AutoML Benchmark that contain at least $10\,000$ samples. The list of datasets used can be found in Table \ref{table:openml_datasets_table}. % TODO: After review cite Automl openml benchmark
Datasets with more than 100 features are limited to the first 100 features. When more than 10 classes are contained in the datasets, samples with any but the first 10 classes are discarded. 
\begin{table}[ht]
    \centering
    \caption{Evaluation datasets for model generalization experiments.}
    \tiny
    \input{figures/benchmark_details/generalization_open_ml_datasets_table}
    \label{table:openml_datasets_table}
\end{table}
\subsection{Ablation on Tuning Differentiable Hyperparameters for Tabular Datasets}
\label{app:ssec:abl_diff}
We added a second variant of the TabPFN for comparison to Figure \ref{fig:perfovertime}, where we replaced the searched hyperparameter embedding with a random hyperparameter embedding. One can see that, while differentiable tuning helps, our strong performance cannot be explained by an overfitting to the distribution of validation datasets during the differentiable tuning. Also, Table \ref{table:diff_ablation} indicates that the ROC AUC performance of our method is influenced less by our differentiable prior tuning than the cross entropy score. This makes a lot of sense, as the metric we optimize via gradient-descent is cross entropy. 
\begin{table}[ht]
    \centering
    \caption{A comparison between of \methodname{} with a tuned and a non-tuned hyperparameter embedding.}
    \tiny
    \input{figures/ablations/diff_ablation}
    \label{table:diff_ablation}
\end{table}
\subsection{Ablation on Using Test Statistics for Normalization} \label{app:abl_train_scaling}
Here we investigate the effect of using training and testing samples for the normalization of input features jointly as described in Appendix \ref{app:preprocessing} as compared to using training samples alone. Table \ref{table:abl_train_scaling} shows that using training and test-set jointly yields only marginally improved results.
\begin{table}[ht]
    \centering
    \caption{A comparison between of \methodname{} using training and testing samples jointly for normalization versus using only training samples.}
    \tiny
    \input{figures/ablations/abl_train_scaling}
    \label{table:abl_train_scaling}
\end{table}
\section{Code}\label{app:code}

Our code alongside notebooks to reproduce our experiments and pretrained models are available at 
ANONYMIZED.
\begin{table}[ht]
    \caption{Datasets used for the evaluation.}
    \label{table:test_datasets_table}
    \centering
    \tiny
    \input{figures/benchmark_details/test_datasets_table}
\end{table}
\begin{table}[ht]
    \centering
    \caption{Meta-Datasets used for tuning the prior.}
    \tiny
    \input{figures/benchmark_details/reference_datasets_table}
    \label{table:reference_datasets_table}
\end{table}
\begin{table}[ht]
    \centering
    \caption{Meta-Datasets used for tuning the prior (continued).}
    \tiny
    \input{figures/benchmark_details/reference_datasets_table_2}
    \label{table:reference_datasets_table_2}
\end{table}

\end{document}